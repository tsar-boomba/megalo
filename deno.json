{
	"fmt": {
		"options": {
			"useTabs": true,
			"singleQuote": true
		}
	},
	"lint": {
		"rules": {
			"exclude": ["ban-types", "no-explicit-any"]
		}
	},
	"tasks": {
		"bench": "deno run -A --unstable dev.ts & sleep 1 && oha -z 30s http://127.0.0.1:9000/",
		"bench:regex": "deno run -A --unstable dev.ts & sleep 1 && oha -z 30s http://127.0.0.1:9000/regex/path",
		"bench:pattern": "deno run -A --unstable dev.ts & sleep 1 && oha -z 30s http://127.0.0.1:9000/pattern/1"
	}
}
